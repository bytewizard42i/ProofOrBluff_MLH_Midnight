node_modules
dist/
build/
midnight-local-dev/
# .env is committed for realDeal/cli/ (public local-dev test seeds —
# see realDeal/docs/LOCAL_DEV_GUIDE.md). For all other paths .env
# stays ignored.
/.env
demoLand/.env
realDeal/app/.env
.env.local
*.log
.DS_Store
*.swp
.idea/
.vscode/
*:Zone.Identifier

# Large original screen-recordings — keep only the compressed version
/media/prooforbluff mini vid.mp4
# Windows alternate-data-stream metadata that leaks into WSL via /mnt/c
*:Zone.Identifier
